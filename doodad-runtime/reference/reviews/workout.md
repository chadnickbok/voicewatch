# Powerlifting parity review

Reviewed 2026-08-01 against the square Wear OS 7 oracle and the production
LVGL renderer.

## Disposition

**Structurally equivalent; raster-equivalent by policy.** The Wasm-owned
powerlifting flow now covers all twelve approved states: Today, session,
exercise selection, active set, weight editing, set result/RPE, missed-set
recovery, rest, plate loading, exercise switching, summary/PR, and durable
resume.

Every state renders from the same SceneSnapshot in Wear Compose and LVGL.
Parallax reports exact node order and normalized bounds for every compared
node, with zero quality findings in either renderer. Raw pixel identity is not
required because Compose and LVGL retain their platform font rasterizers,
antialiasing, and RGB565 conversion paths.

The app launches directly into useful workout content and does not reserve a
generic app-title bar.

## Contract and implementation

- The Wasm guest in `apps/workout/src/lib.rs` owns navigation, weight, reps,
  RPE, recovery, persistence requests, and resume behavior.
- The twelve semantic AppSpecs are generated by
  `tools/powerlifting_foundations/generate_appspecs.py`.
- Generic controls remain AppSpec text, card, progress, stepper, row, and
  button nodes.
- The bounded domain visuals are the set target, previous performance, RPE
  selector, rest-next-set, plate diagram, and actual-versus-target treatment.
- Compose and LVGL share one 192dp coordinate contract on the 240px square
  profile. No renderer infers workout state.

## Decisive trace

The checked-in decisive trace contains 19 accepted renderer operations and 20
checkpoints. Fresh-process replay reaches every screen without invoking Wasm.
Its current trace SHA-256 is
`132a7b7a9bdbb7cbcb7ca38a2ebf0480918e712a0e0ba502fc0087a94420a861`.

## Parallax result

Command:

```bash
./doodad perfect-render workout \
  --profile watch_square_240 \
  --output target/parallax/workout-powerlifting-final
```

Result:

- 12 aligned cases
- 0 structured mismatches
- 0 Compose quality findings
- 0 LVGL quality findings
- 48dp minimum semantic touch target throughout
- changed-pixel fraction from 13.1597% to 35.9236%
- mean absolute error from 12.0341 to 19.4310

The larger raster delta belongs to the result/RPE screen, where Material text
and the four-part segmented selector have the most edge and glyph coverage.
It is not a layout divergence: its semantic tree and bounds are exact.

The review contact sheet is stored at
`reference/powerlifting-foundations/evidence/parallax-contact-sheet.png`.
Pairs are ordered Compose first, LVGL second.

## Real Wear OS evidence

The active-set snapshot was also installed and rendered on the running square
Wear emulator:

- Wear OS 7 / API 37
- build `CP2A.260330.028.E2`
- `wm size 240x240`
- `wm density 200` (192 x 192dp logical viewport)
- Wear Compose 1.6.2

The framebuffer is stored at
`reference/powerlifting-foundations/evidence/wear-emulator-active-set.png` and
has SHA-256
`8768f002be5715160994f7f0ecff99cde19cdfbef4851ad5947cce5880dd7325`.
It matches the host Compose oracle in hierarchy, geometry, color roles, and
type scale.

## Remaining non-blocking work

- Persist the session through the production workout provider rather than the
  deterministic host fixture.
- Add countdown delivery from an absolute rest deadline.
- Add phone/voice routine authoring and program import.
- Validate the same flow on physical CoreS3/T-Watch hardware for frame time,
  memory, haptics, touch quality, and wrist ergonomics.
